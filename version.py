"""App identity — quoted in report headers/footers, window title and
the sidebar brand. NOTE: the QSettings org/app strings and the AI
keystore folder are separate, frozen identifiers — renaming the app
here must never touch those (saved settings would be orphaned)."""

APP_NAME = "Stork Investment"
APP_VERSION = "2.0"
